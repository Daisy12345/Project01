<header>
 	<p class="h2">Banking</p>
	<div>
		<%= link_to "SignUp", signup_path %><span class="tab3"/> 
		<% if current_user %>
			You are logged in as <%= current_user.name %>
			<%= link_to "Log Out", logout_path %>
		<% else %>
			<%= link_to "Log In", login_path %>
		<% end %>
	</div>
</header>
